/*
    Write a program using a basic function (ie., no parameters or return values)
    that prints some string output to console.
*/

#include<iostream>

void printMessage();

int main()
{
    printMessage();

    return 0;
}

void printMessage()
{
    std::cout<<"A basic function has no parameters or return values, yet can print strings to console.";
}
